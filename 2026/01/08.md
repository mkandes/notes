# How to set group-readable file and directory permissions recursively

```
find /path/to/directory -type f -exec chmod -R 644 {} \;
```

```
find /path/to/directory -type d -exec chmod -R 755 {} \;
```
