# hpcGPT @ SDSC - Planning Meeting - Initial [Internal] User Testing

### Illinois Chat Deployment
- May need a persistent volume claim in Voyager Ceph
- Paul will be back around the end of October / early November.

### OpenCode Deployment
- Does OOD have an OpenCode app yet?

### Debugging

Log from frontend when mcp server queries, then manual curl query ...

```
validateApiKeyAndRetrieveData data []
Received /chat request for:  undefined
validateApiKeyAndRetrieveData data [ { email: 'testuser@example.com' } ]
isValidApiKey true
Using UIUC Chat config: use raw SQL to fetch user data from keycloak.user_entity.
Received /chat request for:  testuser@example.com
No N8N API key found, can't fetch tools
Before context search: {
  courseName: 'sdsctest-072326',
  searchQuery: 'What is Expanse?',
  documentGroups: [ 'All Documents' ]
}
p4rodrig@vgr-1-20:~/
```

# References
