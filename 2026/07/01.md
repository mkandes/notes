# MLPerf Inference Image Classification Reference Implementation

```
galyleo launch --account use300 --partition ind-gpu --cpus 40 --memory 368 --gpus 4 --time-limit 04:00:00 --conda-yml mlperf-inference-vision-image-classification-resnet-imagenet-onnxruntime-cpu.yaml --quiet
```

```
export MODEL_DIR="${HOME}/projects/rehs/2026/mlperf/inference/datacenter/resnet"
```

```
export REFERENCE_DATA_DIR="${HOME}/data/imagenet/ilsvrc/2012"
```

```
export LOCAL_SCRATCH_DIR="${SLURM_TMPDIR}"
```

```
mkdir -p "${LOCAL_SCRATCH_DIR}/ILSVRC2012_img_val"
```


```
time -p tar -xf "${REFERENCE_DATA_DIR}/ILSVRC2012_img_val.tar" -C "${LOCAL_SCRATCH_DIR}/ILSVRC2012_img_val"
```

```
cp "${REFERENCE_DATA_DIR}/val_map.txt" "${LOCAL_SCRATCH_DIR}/ILSVRC2012_img_val"
```

```
export DATA_DIR="${LOCAL_SCRATCH_DIR}/ILSVRC2012_img_val"
```

```
cd "${LOCAL_SCRATCH_DIR}"
```

```
git clone https://github.com/mlcommons/inference.git
```

```
cd inference/vision/classification_and_detection/
```

```
export OMP_NUM_THREADS=n
```

```
export OMP_PLACES=cores
```

```
export OMP_PROC_BIND=close
```


```
export OMP_WAIT_POLICY=PASSIVE
```

```
export ORT_DISABLE_THREAD_AFFINITY=1
```

```
export ORT_SESSION_THREAD_POOL_SIZE=n
```

```
srun -n 1 -c 2 ./run_local.sh onnxruntime resnet50 gpu --scenario Offline --max-batchsize 512 --threads 2 --qps 2048
```

```
galyleo launch --account use300 --partition ind-compute --cpus 128 --memory 242 --time-limit 04:00:00 --conda-yml mlperf-inference-vision-image-classification-resnet-imagenet-onnxruntime-cpu.yaml --quiet
```

```
./run_local.sh onnxruntime resnet50 cpu --scenario Offline --max-batchsize 1024 --threads 32 --qps 1024
```

# Reference(s):
- https://oliviajain.github.io/onnxruntime/docs/performance/tune-performance.html
- https://docs.nvidia.com/deeplearning/tensorrt/latest/installing-tensorrt/install-pip.html
- https://infohub.delltechnologies.com/en-uk/p/running-the-mlperf-tm-inference-v1-0-benchmark-on-dell-emc-systems/
