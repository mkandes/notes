# How to run Ollama on Expanse (within a JupyterLab session)

The easiest and most secure way to launch a JupyterLab session on Expanse is via [galyleo](https://github.com/sdsc/galyleo). Here, let's start by creating a a very basic conda envrionment file named `jupyter.yaml`.

```
name: jupyter

channels:
  - conda-forge

dependencies:
  - jupyterlab
```

Next, put `galyleo` in your `PATH`.

```
export PATH="/cm/shared/apps/sdsc/galyleo:${PATH}"
```

Then launch the JupyterLab session within the conda environment on a GPU node within the `gpu-debug` partition. 

```
galyleo launch --account ddp324 --partition gpu-debug --cpus 10 --memory 92 --gpus 1 --time-limit 00:30:00 --conda-yml jupyter.yaml --quiet
```

Copy and paste the URL provided into your web broswer and wait for the job to start up.

```
https://sureness-careless-coral.expanse-user-content.sdsc.edu/?token=XXXXXXXXXXXXXXXXXXXXXX
```

```
[mkandes@login02 envs]$ squeue --me
             JOBID PARTITION     NAME     USER ST       TIME  NODES NODELIST(REASON)
          52031675   compute galyleo-  mkandes  R      17:43      1 exp-16-55
          52032399 gpu-debug galyleo-  mkandes  R       0:36      1 exp-7-59
[mkandes@login02 envs]$
```

Once the JupyterLab session begins, open its Terminal app within the Launcher window. You now have a basic conda environment where you can iterate on building any software environment. e.g., your Presidio environment. Unfortuantely, right now it looks like the latest Ollama distributions from `conda-forge` are effectively broken and do not properly support NVIDIA GPUs out of the box. There may be some explaination for this in the maintainers changelog, but we can also simply download a fully functional Ollama distribution from the project itself. 

First, create a software directory in your `$HOME` directory to store Ollama.

```
mkdir -p ~/software/ollama
```

Next, change directories to this location.

```
cd ~/software/ollama
```

Then download the latest Ollama distribution via `wget`.

```
wget https://github.com/ollama/ollama/releases/download/v0.32.0/ollama-linux-amd64.tar.zst
```

Extract the compressed archive file.

```
tar --use-compress-program=unzstd -xvf ollama-linux-amd64.tar.zst
```

You can then place Ollama into your software environment using the `PATH` and `LD_LIBRARY_PATH` environment variables.

```
export PATH="${HOME}/software/ollama/bin:${PATH}"
```

```
export LD_LIBRARY_PATH="${HOME}/software/ollama/lib/ollama:${LD_LIBRARY_PATH}"
```

```
export LD_LIBRARY_PATH="${HOME}/software/ollama/lib/ollama/cuda_v13:${LD_LIBRARY_PATH}"
```

You should now be able to serve Ollama.

```
ollama serve
```

Once the server starts up, you'll want to open a new Terminal session using the `+` button next to the top tab of your current Terminal session. In the new Terminal session, set up the `PATH` and `LD_LIBRARY_PATH` environment variables again, then start by running the `gemma4:31b` model.

```
export PATH="${HOME}/software/ollama/bin:${PATH}"
```

```
export LD_LIBRARY_PATH="${HOME}/software/ollama/lib/ollama:${LD_LIBRARY_PATH}"
```

```
export LD_LIBRARY_PATH="${HOME}/software/ollama/lib/ollama/cuda_v13:${LD_LIBRARY_PATH}"
```

```
ollama run gemma4:31b
```

You can also save the `PATH` and `LD_LIBRARY_PATH` changes in your `~/.bashrc` file so you don't have to keep setting these environment variables each time you start a new session. 

Here is my example output from `gemma4:31b`. 

```
[mkandes@exp-7-59 ~]$ ollama run gemma4:31b
bash: ollama: command not found
[mkandes@exp-7-59 ~]$ export PATH="${HOME}/software/ollama/bin:${PATH}"
[mkandes@exp-7-59 ~]$ export LD_LIBRARY_PATH="${HOME}/software/ollama/lib/ollama:${LD_LIBRARY_PATH}"
[mkandes@exp-7-59 ~]$ export LD_LIBRARY_PATH="${HOME}/software/ollama/lib/ollama/cuda_v13:${LD_LIBRARY_PATH}"
[mkandes@exp-7-59 ~]$ ollama run gemma4:31b
>>> Tell me how to use Microsoft Presidio.
Thinking...
```

Remember, you can always confirm this installation is taking advantage of your GPU resources by logginng in to the compute node via `ssh` while the job is running.

```
[mkandes@login02 envs]$ squeue --me
             JOBID PARTITION     NAME     USER ST       TIME  NODES NODELIST(REASON)
          52031675   compute galyleo-  mkandes  R      40:40      1 exp-16-55
          52032399 gpu-debug galyleo-  mkandes  R      23:33      1 exp-7-59
[mkandes@login02 envs]$ ssh exp-7-59
Last login: Mon Jul 13 19:29:50 2026 from 10.21.0.20
[mkandes@exp-7-59 ~]$
```

Once logged in, you can run `nvidia-smi` and poll the status of the GPUs.

```
nvidia-smi -l 1
```

You should then see the GPU load spike when you are conversing with the models. 

Note: Another model I think I'd be interested in testing as a backend to Presidio's LLM recognizer feature would be Olmo-3. But feel free to explore any model you think might be useful for the NER task we are interested in.

```
[mkandes@exp-7-59 ~]$ ollama run olmo-3:32b-think
pulling manifest 
pulling e7b4ff6dfb04: 100% ▕████████████████████████████████████████████████████████████████████████████████████████████████▏  19 GB                         
pulling cfc7749b96f6: 100% ▕████████████████████████████████████████████████████████████████████████████████████████████████▏  11 KB                         
pulling ceaa5f78afb8: 100% ▕████████████████████████████████████████████████████████████████████████████████████████████████▏   33 B                         
pulling 3a62000b5620: 100% ▕████████████████████████████████████████████████████████████████████████████████████████████████▏  466 B                         
verifying sha256 digest 
writing manifest 
success 
>>> How do I use Presidio's LLM recognizer backend?
Thinking...
```

Once you've tested everything in the `gpu-debug` partition, feel free to move to the `gpu-shared` partition, where you can request `--time` longer than 30 minutes. You should also simply include your installation of Presidio in the environment as well when ready to combine Presidio with Ollama and use JupyterLab as your development environment.


# References
- https://github.com/sdsc/galyleo
