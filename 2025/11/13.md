# Using Mac OS X built-in VNC Client

1. In the Finder , open the `/Applications/Utilities` folder, then double-click `Terminal`.
2. Once you open a `Terminal` window, login to Expanse via SSH and submit an interactive job with the resources you want to use.
3. ...
4. ...
